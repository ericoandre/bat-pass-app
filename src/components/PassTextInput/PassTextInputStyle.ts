import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
inputer:{
    backgroundColor: '#E5BF3C',
    color: '#fefefeff',
    width:'80%' ,
    fontSize:20,
    height:40,
    // borderWidth:1,
    borderRadius:5,
    padding:10,
    textAlign:'center'
  }
});